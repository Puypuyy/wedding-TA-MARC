import SharedOnepager from "../SharedOnepager";

const OnepagerDesktop = ({ onBackToInvitation }) => {
  return <SharedOnepager onBackToInvitation={onBackToInvitation} variant="desktop" />;
};

export default OnepagerDesktop;
