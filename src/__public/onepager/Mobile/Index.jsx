import SharedOnepager from "../SharedOnepager";

const OnepagerMobile = ({ onBackToInvitation }) => {
  return <SharedOnepager onBackToInvitation={onBackToInvitation} variant="mobile" />;
};

export default OnepagerMobile;
